package {{packageName}}.entity

import javax.persistence.Column;
import javax.persistence.Entity;
import javax.persistence.GeneratedValue;
import javax.persistence.Id;
import javax.persistence.Table;
import javax.persistence.GenerationType;

{{#isEntity}}
@Entity
@Table(name="{{tableName}}")
{{/isEntity}}
public class {{ClassName}} implements Serializable{



	{{#properties}}
		{{#primaryKey}}@Id{{/primaryKey}}{{#isAutoGenerated}}@GeneratedValue(strategy=GenerationType.AUTO){{/isAutoGenerated}}{{> entityrelationshiptypes}}		
		@Column(name="{{propertyName}}")		
		private {{> entityreturntypes}} {{propertyName}};	
	
	{{/properties}}
	
	
	public {{ClassName}}(){
	
	}
	
	{{#properties}}
		public {{> entityreturntypes}} get{{PropertyName}}(){
		
			return this.{{propertyName}};
		}
		public {{> entityreturntypes}} set{{PropertyName}}({{> entityreturntypes}} {{propertyName}}){
		
			return this.{{propertyName}}={{propertyName}};
		}
	{{/properties}}

	
	


}